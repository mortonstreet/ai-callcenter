{
  "name": "revcenter",
  "version": "1.0.0",
  "description": "RevCenter - Your AI call center agent platform",
  "main": "index.js",
  "private": true,
  "scripts": {
    "format": "prettier  \"{backend, frontend, shared}/**/*.{ts,tsx,js,json}\" --ignore-path .gitignore",
    "format:check": "pnpm format --check",
    "format:write": "pnpm format --write",
    "revcenter:dev": "node scripts/revcenter-dev.mjs",
    "worktrees:list": "git worktree list",
    "worktrees:prune": "git worktree prune",
    "worktrees:audit": "bash ./scripts/worktree-audit.sh",
    "build": "pnpm --dir backend build && pnpm --dir frontend build && pnpm --dir revcenter-cli build",
    "tsc": "pnpm --dir backend tsc && pnpm --dir frontend exec tsc --noEmit && pnpm --dir revcenter-cli tsc",
    "lint": "pnpm --dir frontend lint",
    "validate:precommit": "pnpm format:write && pnpm tsc && pnpm lint",
    "validate": "pnpm format:write && pnpm tsc && pnpm lint && pnpm build",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.19.0",
  "pnpm": {
    "overrides": {
      "prisma": "^6.17.1",
      "@prisma/client": "^6.17.1"
    }
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^16.2.5",
    "nx": "21.6.5",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
